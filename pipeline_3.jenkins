pipeline {
   agent any

   stages {
      stage ('Set up env') {
         steps {
            sh "pip3 install -r requirement.txt"
            sh "pip3 list"
               }
      }
      stage('runpy') {
        steps{      
          sh 'python3 test_test_DB.py'
        }
        }

  }
}
